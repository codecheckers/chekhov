# Copy to .env (gitignored) and fill in. Never commit real tokens.

# Fine-grained personal access token of the bot user, created with
#   resource owner:     codecheckers
#   repository access:  only codecheckers/testing-dev-register
#   permissions:        Issues read and write, Contents read, Metadata read
#                       (Pull requests read and write once `register` exists)
# See docs/github-token.md, which also says why this is not a classic token.
CHEKHOV_GH_ACCESS_TOKEN=

# Shared secret, must match the webhook secret configured on the target repo.
# Invent one: openssl rand -hex 32
CHEKHOV_GH_SECRET_TOKEN=

# Access token of an application on the codecheck account at fediscience.org,
# with the scopes read:accounts read:statuses write:media write:statuses
# read:search read:follows write:follows read:collections write:collections
# (codecheckers/chekhov#31).
# Optional: without it "@chekhovbot announce" previews but does not post, and
# "@chekhovbot follow" only ever previews.
# See docs/mastodon-token.md.
CHEKHOV_MASTODON_TOKEN=
# The account and instance the token belongs to (defaults in the settings file)
CHEKHOV_MASTODON_ACCOUNT=codecheck
CHEKHOV_MASTODON_INSTANCE=https://fediscience.org

# Bot account and target repo (defaults are in config/settings-development.yml)
CHEKHOV_BOT_GH_USER=chekhovbot
CHEKHOV_TARGET_REPO=codecheckers/testing-dev-register

# development | production
CHEKHOV_ENV=development

# The key the bot signs the record of a check with, so it can tell when
# somebody with write access has edited it. Make one with
# `go run ./cmd/chekhov record-key`; without it records are written unsigned
# and an edit goes unnoticed. See docs/record-key.md.
CHEKHOV_RECORD_KEY=
# Public keys that should still verify after a rotation, comma-separated.
CHEKHOV_RECORD_KEYS_RETIRED=
